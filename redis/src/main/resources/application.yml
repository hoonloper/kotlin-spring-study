spring:
  application:
    name: redis
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
      timeout: 5000
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 1